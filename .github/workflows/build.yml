name: Build EXE

on:
  workflow_dispatch:
  push:
    paths:
      - timesheet_transformer.py
      - requirements.txt
      - .github/workflows/build.yml

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

     - name: Build windowed EXE (no console)
  run: |
    pyinstaller -F -w -i icon.ico -n TabelConverter timesheet_transformer.py

- name: Build console EXE (with console)
  run: |
    pyinstaller -F -i icon.ico -n TabelConverter_console timesheet_transformer

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: TimesheetTransformer
          path: |
            dist/TimesheetTransformer.exe
            dist/TimesheetTransformer_console.exe
          if-no-files-found: error
